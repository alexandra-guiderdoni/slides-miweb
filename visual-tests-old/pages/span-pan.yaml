name: "SPAN / PAN - accessibilité numérique"
description: "Auto-generated ShipGuard static HTML route for span-pan/index.html"
priority: medium
requires_auth: false
timeout: 30s
tags: [auto-generated, static-html, miweb]

steps:
  - action: open
    url: "{base_url}/span-pan/"
  - action: llm-check
    description: "Page loads and renders content"
    criteria: "Page content is visible, no blank screen, no browser error, no broken primary layout, no visibly missing slide image where one is expected."
    severity: critical
    screenshot: "span-pan.png"
